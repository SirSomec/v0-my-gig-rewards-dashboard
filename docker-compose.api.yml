# Оставлен для обратной совместимости. Сервисы api и db теперь входят в основной docker-compose.yml.
# Если нужен только фронт без бэкенда — в docker-compose.yml можно закомментировать сервисы api и db.
# Этот файл больше не требуется для полного стека.
